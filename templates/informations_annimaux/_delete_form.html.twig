<form method="post" action="{{ path('app_informations_annimaux_delete', {'id': informations_annimaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informations_annimaux.id) }}">
    <button class="btn">Delete</button>
</form>
