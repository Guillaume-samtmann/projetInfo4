<form method="post" action="{{ path('app_informations_horraire_arv_delete', {'id': informations_horraire_arv.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informations_horraire_arv.id) }}">
    <button class="btn">Delete</button>
</form>
