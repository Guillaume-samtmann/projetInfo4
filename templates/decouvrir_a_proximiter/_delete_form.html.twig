<form method="post" action="{{ path('app_decouvrir_a_proximiter_delete', {'id': decouvrir_a_proximiter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decouvrir_a_proximiter.id) }}">
    <button class="btn">Delete</button>
</form>
