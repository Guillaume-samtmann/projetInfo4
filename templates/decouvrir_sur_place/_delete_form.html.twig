<form method="post" action="{{ path('app_decouvrir_sur_place_delete', {'id': decouvrir_sur_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decouvrir_sur_place.id) }}">
    <button class="btn">Delete</button>
</form>
