{% extends 'base.html.twig' %}

{% block title %}DecouvrirSurPlace{% endblock %}

{% block body %}
    <h1>DecouvrirSurPlace</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decouvrir_sur_place.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ decouvrir_sur_place.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_decouvrir_sur_place_index') }}">back to list</a>

    <a href="{{ path('app_decouvrir_sur_place_edit', {'id': decouvrir_sur_place.id}) }}">edit</a>

    {{ include('decouvrir_sur_place/_delete_form.html.twig') }}
{% endblock %}
