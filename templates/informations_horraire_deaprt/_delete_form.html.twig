<form method="post" action="{{ path('app_informations_horraire_deaprt_delete', {'id': informations_horraire_deaprt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informations_horraire_deaprt.id) }}">
    <button class="btn">Delete</button>
</form>
