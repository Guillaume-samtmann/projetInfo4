{% extends 'base.html.twig' %}

{% block title %}MotCles{% endblock %}

{% block body %}
    <h1>MotCles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mot_cle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mot_cle.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mot_cles_index') }}">back to list</a>

    <a href="{{ path('app_mot_cles_edit', {'id': mot_cle.id}) }}">edit</a>

    {{ include('mot_cles/_delete_form.html.twig') }}
{% endblock %}
