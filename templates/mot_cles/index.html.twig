{% extends 'base.html.twig' %}

{% block title %}MotCles index{% endblock %}

{% block body %}
    <h1>MotCles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mot_cle in mot_cles %}
            <tr>
                <td>{{ mot_cle.id }}</td>
                <td>{{ mot_cle.nom }}</td>
                <td>
                    <a href="{{ path('app_mot_cles_show', {'id': mot_cle.id}) }}">show</a>
                    <a href="{{ path('app_mot_cles_edit', {'id': mot_cle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_mot_cles_new') }}">Create new</a>
{% endblock %}
